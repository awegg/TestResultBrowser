@page "/morning-triage"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/configuration-history?mode=triage", replace: true);
    }
}
